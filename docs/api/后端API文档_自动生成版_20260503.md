# 后端API文档

生成时间：2026-05-03 19:23
接口总数：129
服务基址：`http://localhost:7777/api`

## 通用响应结构

| 参数名 | 类型 | 说明 | 示例值 |
| --- | --- | --- | --- |
| `success` | boolean | 是否成功 | `true` |
| `code` | string | 业务状态码 | `"200"` |
| `message` | string | 提示信息 | `"操作成功"` |
| `data` | object/array | 业务数据体 | `{}` |

## 通用异常响应

| 错误码 | 错误信息 | 异常场景 |
| --- | --- | --- |
| `400` | 请求参数错误 | 缺少必填参数、字段类型错误、业务校验失败 |
| `401` | 未授权或登录失效 | 未携带令牌、令牌失效 |
| `403` | 无权限访问 | 角色权限不足 |
| `404` | 资源不存在 | 路径参数对应记录不存在 |
| `405` | 请求方法不支持 | 请求方式与控制器定义不一致 |
| `500` | 系统异常，请联系管理员 | 未捕获异常、服务内部错误 |

## 目录

- [AIChatHistoryController](#aichathistorycontroller)（4 个接口）
- [AIController](#aicontroller)（2 个接口）
- [AddressController](#addresscontroller)（6 个接口）
- [AdminAnnouncementController](#adminannouncementcontroller)（8 个接口）
- [AdminController](#admincontroller)（5 个接口）
- [AgentMonitoringController](#agentmonitoringcontroller)（3 个接口）
- [CacheMonitorController](#cachemonitorcontroller)（5 个接口）
- [CategoryController](#categorycontroller)（2 个接口）
- [ContentExtractionController](#contentextractioncontroller)（9 个接口）
- [CouponController](#couponcontroller)（5 个接口）
- [DishStepController](#dishstepcontroller)（9 个接口）
- [FestivalController](#festivalcontroller)（11 个接口）
- [FileAccessController](#fileaccesscontroller)（1 个接口）
- [FileController](#filecontroller)（3 个接口）
- [HomeController](#homecontroller)（3 个接口）
- [HotTopicAdminController](#hottopicadmincontroller)（8 个接口）
- [MockOAuthController](#mockoauthcontroller)（4 个接口）
- [PaymentController](#paymentcontroller)（8 个接口）
- [RemarkConflictController](#remarkconflictcontroller)（4 个接口）
- [SupervisorAgentController](#supervisoragentcontroller)（3 个接口）
- [SupervisorSSEController](#supervisorssecontroller)（2 个接口）
- [TutorialController](#tutorialcontroller)（4 个接口）
- [TutorialMerchantController](#tutorialmerchantcontroller)（5 个接口）
- [TutorialStatisticsController](#tutorialstatisticscontroller)（3 个接口）
- [VerificationController](#verificationcontroller)（2 个接口）
- [WalletSecurityController](#walletsecuritycontroller)（2 个接口）
- [WishListController](#wishlistcontroller)（8 个接口）

## AIChatHistoryController

### 1. AI聊天历史控制器

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | AI聊天历史控制器 |
| 请求地址 | `http://localhost:7777/api/v1/ai/chat/history` |
| 请求方式 | `GET` |
| 接口描述 | AI聊天历史控制器 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | Map<String, Object> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 2. 保存聊天消息 POST /v1/ai/c

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 保存聊天消息 POST /v1/ai/c |
| 请求地址 | `http://localhost:7777/api/v1/ai/chat/save` |
| 请求方式 | `POST` |
| 接口描述 | 保存聊天消息 POST /v1/ai/chat/save |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `params` | Map<T> | 是 | 请求体 | { userId, sender, content } |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | Map<String, Object> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 3. 清空用户聊天记录 DELETE /v1/

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 清空用户聊天记录 DELETE /v1/ |
| 请求地址 | `http://localhost:7777/api/v1/ai/chat/clear` |
| 请求方式 | `DELETE` |
| 接口描述 | 清空用户聊天记录 DELETE /v1/ai/chat/clear |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | Map<String, Object> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 4. 检查用户是否有聊天历史 GET /v1/

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 检查用户是否有聊天历史 GET /v1/ |
| 请求地址 | `http://localhost:7777/api/v1/ai/chat/has-history` |
| 请求方式 | `GET` |
| 接口描述 | 检查用户是否有聊天历史 GET /v1/ai/chat/has-history |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | Map<String, Object> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```


## AIController

### 1. AI食谱优化（SSE流式进度） - 实时

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | AI食谱优化（SSE流式进度） - 实时 |
| 请求地址 | `http://localhost:7777/api/v1/ai/recipe/stream` |
| 请求方式 | `POST` |
| 接口描述 | AI食谱优化（SSE流式进度） - 实时推送优化进度和流式token，桌面端专用 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `params` | Map<T> | 是 | 请求体 | 请求体 |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | SseEmitter | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 2. 健康检查 - 检查AI服务状态

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 健康检查 - 检查AI服务状态 |
| 请求地址 | `http://localhost:7777/api/v1/ai/health` |
| 请求方式 | `GET` |
| 接口描述 | 健康检查 - 检查AI服务状态 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```


## AddressController

### 1. 用户地址控制器

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 用户地址控制器 |
| 请求地址 | `http://localhost:7777/api/v1/addresses/user` |
| 请求方式 | `GET` |
| 接口描述 | 用户地址控制器 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 2. 获取默认地址（兼容前端API）

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 获取默认地址（兼容前端API） |
| 请求地址 | `http://localhost:7777/api/v1/addresses/default` |
| 请求方式 | `GET` |
| 接口描述 | 获取默认地址（兼容前端API） |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 3. 添加地址（兼容前端API）

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 添加地址（兼容前端API） |
| 请求地址 | `http://localhost:7777/api/v1/addresses` |
| 请求方式 | `POST` |
| 接口描述 | 添加地址（兼容前端API） |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `address` | Address | 是 | 请求体 | 请求体 |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 4. 更新地址（兼容前端API）

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 更新地址（兼容前端API） |
| 请求地址 | `http://localhost:7777/api/v1/addresses/{id}` |
| 请求方式 | `PUT` |
| 接口描述 | 更新地址（兼容前端API） |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 5. 删除地址（兼容前端API）

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 删除地址（兼容前端API） |
| 请求地址 | `http://localhost:7777/api/v1/addresses/{id}` |
| 请求方式 | `DELETE` |
| 接口描述 | 删除地址（兼容前端API） |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 6. 设置默认地址（兼容前端API）

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 设置默认地址（兼容前端API） |
| 请求地址 | `http://localhost:7777/api/v1/addresses/{id}/default` |
| 请求方式 | `PUT` |
| 接口描述 | 设置默认地址（兼容前端API） |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```


## AdminAnnouncementController

### 1. 管理员-公告管理控制器

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 管理员-公告管理控制器 |
| 请求地址 | `http://localhost:7777/api/admin/announcements` |
| 请求方式 | `GET` |
| 接口描述 | 管理员-公告管理控制器 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `defaultValue` | = | 是 | 查询参数 | - |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `records` | array | 数据列表 |
| `total` | integer | 总记录数 |
| `current` | integer | 当前页码 |
| `size` | integer | 每页大小 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "defaultValue": {}
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 2. 获取公告详情

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 获取公告详情 |
| 请求地址 | `http://localhost:7777/api/admin/announcements/{announcementId}` |
| 请求方式 | `GET` |
| 接口描述 | 获取公告详情 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | ResponseEntity<Map<String, Object>> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 3. 创建公告

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 创建公告 |
| 请求地址 | `http://localhost:7777/api/admin/announcements` |
| 请求方式 | `POST` |
| 接口描述 | 创建公告 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `request` | Map<T> | 是 | 请求体 | 请求体 |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | ResponseEntity<Map<String, Object>> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 4. 更新公告

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 更新公告 |
| 请求地址 | `http://localhost:7777/api/admin/announcements/{announcementId}` |
| 请求方式 | `PUT` |
| 接口描述 | 更新公告 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `request` | Map<T> | 是 | 请求体 | 请求体 |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | ResponseEntity<Map<String, Object>> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 5. 删除公告

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 删除公告 |
| 请求地址 | `http://localhost:7777/api/admin/announcements/{announcementId}` |
| 请求方式 | `DELETE` |
| 接口描述 | 删除公告 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | ResponseEntity<Map<String, Object>> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 6. 批量删除公告

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 批量删除公告 |
| 请求地址 | `http://localhost:7777/api/admin/announcements/batch` |
| 请求方式 | `DELETE` |
| 接口描述 | 批量删除公告 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `announcementIds` | java.util.List<T> | 是 | 请求体 | 请求体 |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | ResponseEntity<Map<String, Object>> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 7. 发布/下线公告（修改状态）

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 发布/下线公告（修改状态） |
| 请求地址 | `http://localhost:7777/api/admin/announcements/{announcementId}/status` |
| 请求方式 | `PUT` |
| 接口描述 | 发布/下线公告（修改状态） |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `request` | Map<T> | 是 | 请求体 | 请求体 |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | ResponseEntity<Map<String, Object>> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 8. 获取公告统计

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 获取公告统计 |
| 请求地址 | `http://localhost:7777/api/admin/announcements/statistics` |
| 请求方式 | `GET` |
| 接口描述 | 获取公告统计 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | ResponseEntity<Map<String, Object>> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```


## AdminController

### 1. 管理员管理控制器

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 管理员管理控制器 |
| 请求地址 | `http://localhost:7777/api/admin/list` |
| 请求方式 | `GET` |
| 接口描述 | 管理员管理控制器 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `records` | array | 数据列表 |
| `total` | integer | 总记录数 |
| `current` | integer | 当前页码 |
| `size` | integer | 每页大小 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 2. 创建管理员

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 创建管理员 |
| 请求地址 | `http://localhost:7777/api/admin/{adminId}/status` |
| 请求方式 | `PUT` |
| 接口描述 | 创建管理员 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `request` | Map<T> | 是 | 请求体 | 请求体 |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | ResponseEntity<Map<String, Object>> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 3. 重置管理员密码

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 重置管理员密码 |
| 请求地址 | `http://localhost:7777/api/admin/current` |
| 请求方式 | `GET` |
| 接口描述 | 重置管理员密码 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | ResponseEntity<Map<String, Object>> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 4. 更新当前管理员个人信息

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 更新当前管理员个人信息 |
| 请求地址 | `http://localhost:7777/api/admin/profile` |
| 请求方式 | `PUT` |
| 接口描述 | 更新当前管理员个人信息 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `updateData` | Map<T> | 是 | 请求体 | 请求体 |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | ResponseEntity<Map<String, Object>> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 5. 上传管理员头像 - Base64格式

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 上传管理员头像 - Base64格式 |
| 请求地址 | `http://localhost:7777/api/admin/profile/avatar/base64` |
| 请求方式 | `PUT` |
| 接口描述 | 上传管理员头像 - Base64格式 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `base64Data` | Map<T> | 是 | 请求体 | 请求体 |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | ResponseEntity<Map<String, Object>> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```


## AgentMonitoringController

### 1. Agent监控控制器 提供监控数据查询和

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | Agent监控控制器 提供监控数据查询和 |
| 请求地址 | `http://localhost:7777/api/api/admin/agent-monitoring/call-chain/{sessionId}` |
| 请求方式 | `GET` |
| 接口描述 | Agent监控控制器 提供监控数据查询和调用链追踪的API 注意：此Controller暂时禁用，等待监控系统配置完善后启用 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 2. 清理过期调用链

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 清理过期调用链 |
| 请求地址 | `http://localhost:7777/api/api/admin/agent-monitoring/call-chain/cleanup` |
| 请求方式 | `POST` |
| 接口描述 | 清理过期调用链 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `defaultValue` | = | 是 | 查询参数 | - |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "defaultValue": {}
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 3. 获取监控概览

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 获取监控概览 |
| 请求地址 | `http://localhost:7777/api/api/admin/agent-monitoring/overview` |
| 请求方式 | `GET` |
| 接口描述 | 获取监控概览 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```


## CacheMonitorController

### 1. 缓存监控Dashboard Contro

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 缓存监控Dashboard Contro |
| 请求地址 | `http://localhost:7777/api/admin/cache/overview` |
| 请求方式 | `GET` |
| 接口描述 | 缓存监控Dashboard Controller 提供简单的Web API访问缓存监控数据 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | Map<String, Object> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 2. 获取详细统计

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 获取详细统计 |
| 请求地址 | `http://localhost:7777/api/admin/cache/details` |
| 请求方式 | `GET` |
| 接口描述 | 获取详细统计 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | Map<String, Object> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 3. 获取文本报告

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 获取文本报告 |
| 请求地址 | `http://localhost:7777/api/admin/cache/report` |
| 请求方式 | `GET` |
| 接口描述 | 获取文本报告 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | Map<String, String> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 4. 重置统计

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 重置统计 |
| 请求地址 | `http://localhost:7777/api/admin/cache/reset` |
| 请求方式 | `GET` |
| 接口描述 | 重置统计 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | Map<String, String> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 5. 获取健康状态

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 获取健康状态 |
| 请求地址 | `http://localhost:7777/api/admin/cache/health` |
| 请求方式 | `GET` |
| 接口描述 | 获取健康状态 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | Map<String, Object> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```


## CategoryController

### 1. 经营品类控制器

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 经营品类控制器 |
| 请求地址 | `http://localhost:7777/api/v1/category/common` |
| 请求方式 | `GET` |
| 接口描述 | 经营品类控制器 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 2. 获取所有品类接口 用于搜索功能

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 获取所有品类接口 用于搜索功能 |
| 请求地址 | `http://localhost:7777/api/v1/category/list` |
| 请求方式 | `GET` |
| 接口描述 | 获取所有品类接口 用于搜索功能，返回所有可用的品类 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```


## ContentExtractionController

### 1. 内容提取控制器

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 内容提取控制器 |
| 请求地址 | `http://localhost:7777/api/v1/content-extraction/source` |
| 请求方式 | `POST` |
| 接口描述 | 内容提取控制器 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `dto` | ContentSourceCreateDTO | 是 | 请求体 | 创建DTO |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 2. 获取用户的内容源列表

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 获取用户的内容源列表 |
| 请求地址 | `http://localhost:7777/api/v1/content-extraction/sources` |
| 请求方式 | `GET` |
| 接口描述 | 获取用户的内容源列表 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 3. 获取内容源详情

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 获取内容源详情 |
| 请求地址 | `http://localhost:7777/api/v1/content-extraction/source/{sourceId}` |
| 请求方式 | `GET` |
| 接口描述 | 获取内容源详情 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 4. 获取提取详情

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 获取提取详情 |
| 请求地址 | `http://localhost:7777/api/v1/content-extraction/extraction/{extractionId}` |
| 请求方式 | `GET` |
| 接口描述 | 获取提取详情 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 5. 更新提取内容

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 更新提取内容 |
| 请求地址 | `http://localhost:7777/api/v1/content-extraction/extraction` |
| 请求方式 | `PUT` |
| 接口描述 | 更新提取内容 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `dto` | ContentExtractionUpdateDTO | 是 | 请求体 | 更新DTO |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 6. 发布为食谱

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 发布为食谱 |
| 请求地址 | `http://localhost:7777/api/v1/content-extraction/extraction/{extractionId}/publish` |
| 请求方式 | `POST` |
| 接口描述 | 发布为食谱 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 7. 验证提取内容

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 验证提取内容 |
| 请求地址 | `http://localhost:7777/api/v1/content-extraction/extraction/{extractionId}/verify` |
| 请求方式 | `POST` |
| 接口描述 | 验证提取内容 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `required` | = | 是 | 查询参数 | - |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "required": {}
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 8. 重新提取

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 重新提取 |
| 请求地址 | `http://localhost:7777/api/v1/content-extraction/source/{sourceId}/re-extract` |
| 请求方式 | `POST` |
| 接口描述 | 重新提取 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 9. 删除内容源

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 删除内容源 |
| 请求地址 | `http://localhost:7777/api/v1/content-extraction/source/{sourceId}` |
| 请求方式 | `DELETE` |
| 接口描述 | 删除内容源 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```


## CouponController

### 1. 获取usercoupons

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 获取usercoupons |
| 请求地址 | `http://localhost:7777/api/v1/coupons/user` |
| 请求方式 | `GET` |
| 接口描述 | 获取usercoupons |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 2. 检查couponavailable

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 检查couponavailable |
| 请求地址 | `http://localhost:7777/api/v1/coupons/check` |
| 请求方式 | `POST` |
| 接口描述 | 检查couponavailable |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 3. 暂无描述

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 暂无描述 |
| 请求地址 | `http://localhost:7777/api/v1/coupons/use` |
| 请求方式 | `POST` |
| 接口描述 | 暂无描述 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 4. 暂无描述

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 暂无描述 |
| 请求地址 | `http://localhost:7777/api/v1/coupons/release` |
| 请求方式 | `POST` |
| 接口描述 | 暂无描述 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 5. 暂无描述

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 暂无描述 |
| 请求地址 | `http://localhost:7777/api/v1/coupons/issue-test` |
| 请求方式 | `POST` |
| 接口描述 | 暂无描述 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```


## DishStepController

### 1. 更新dishstep

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 更新dishstep |
| 请求地址 | `http://localhost:7777/api/v1/dish-steps/update` |
| 请求方式 | `POST` |
| 接口描述 | 更新dishstep |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `dto` | DishStepUpdateDTO | 是 | 请求体 | 请求体 |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 2. 暂无描述

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 暂无描述 |
| 请求地址 | `http://localhost:7777/api/v1/dish-steps/batch-update` |
| 请求方式 | `POST` |
| 接口描述 | 暂无描述 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `dto` | DishStepUpdateDTO | 是 | 请求体 | 请求体 |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 3. 回滚dishstep

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 回滚dishstep |
| 请求地址 | `http://localhost:7777/api/v1/dish-steps/rollback` |
| 请求方式 | `POST` |
| 接口描述 | 回滚dishstep |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `dto` | DishStepUpdateDTO | 是 | 请求体 | 请求体 |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 4. 暂无描述

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 暂无描述 |
| 请求地址 | `http://localhost:7777/api/v1/dish-steps/batch-mark` |
| 请求方式 | `POST` |
| 接口描述 | 暂无描述 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 5. 更新stepsort

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 更新stepsort |
| 请求地址 | `http://localhost:7777/api/v1/dish-steps/update-sort` |
| 请求方式 | `POST` |
| 接口描述 | 更新stepsort |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `stepSortItems` | List<T> | 是 | 请求体 | 请求体 |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 6. 获取dishstepdetail

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 获取dishstepdetail |
| 请求地址 | `http://localhost:7777/api/v1/dish-steps/detail/{orderDishId}` |
| 请求方式 | `GET` |
| 接口描述 | 获取dishstepdetail |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 7. 获取orderdishsteps

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 获取orderdishsteps |
| 请求地址 | `http://localhost:7777/api/v1/dish-steps/order/{orderId}` |
| 请求方式 | `GET` |
| 接口描述 | 获取orderdishsteps |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 8. 筛选bystepstatus

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 筛选bystepstatus |
| 请求地址 | `http://localhost:7777/api/v1/dish-steps/filter` |
| 请求方式 | `GET` |
| 接口描述 | 筛选bystepstatus |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 9. 暂无描述

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 暂无描述 |
| 请求地址 | `http://localhost:7777/api/v1/dish-steps/initialize/{orderId}` |
| 请求方式 | `POST` |
| 接口描述 | 暂无描述 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```


## FestivalController

### 1. 节日推荐控制器

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 节日推荐控制器 |
| 请求地址 | `http://localhost:7777/api/v1/festival/active` |
| 请求方式 | `GET` |
| 接口描述 | 节日推荐控制器 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 2. 获取即将到来的节日推荐

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 获取即将到来的节日推荐 |
| 请求地址 | `http://localhost:7777/api/v1/festival/upcoming` |
| 请求方式 | `GET` |
| 接口描述 | 获取即将到来的节日推荐 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `defaultValue` | = | 是 | 查询参数 | - |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "defaultValue": {}
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 3. 根据类型获取节日推荐

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 根据类型获取节日推荐 |
| 请求地址 | `http://localhost:7777/api/v1/festival/type/{festivalType}` |
| 请求方式 | `GET` |
| 接口描述 | 根据类型获取节日推荐 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 4. 获取当前节日推荐（匹配前端API路径）

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 获取当前节日推荐（匹配前端API路径） |
| 请求地址 | `http://localhost:7777/api/v1/festival/recommendations/current` |
| 请求方式 | `GET` |
| 接口描述 | 获取当前节日推荐（匹配前端API路径） |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 5. 根据节日ID获取推荐

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 根据节日ID获取推荐 |
| 请求地址 | `http://localhost:7777/api/v1/festival/recommendations/festival/{festivalId}` |
| 请求方式 | `GET` |
| 接口描述 | 根据节日ID获取推荐 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 6. 获取首页推荐

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 获取首页推荐 |
| 请求地址 | `http://localhost:7777/api/v1/festival/homepage` |
| 请求方式 | `GET` |
| 接口描述 | 获取首页推荐 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `defaultValue` | = | 是 | 查询参数 | - |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "defaultValue": {}
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 7. 添加节日推荐菜品（管理员/商家）

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 添加节日推荐菜品（管理员/商家） |
| 请求地址 | `http://localhost:7777/api/v1/festival/recommend` |
| 请求方式 | `POST` |
| 接口描述 | 添加节日推荐菜品（管理员/商家） |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `dto` | FestivalDishRecommendDTO | 是 | 请求体 | 推荐DTO |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 8. 创建用户自定义事件

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 创建用户自定义事件 |
| 请求地址 | `http://localhost:7777/api/v1/festival/custom-event` |
| 请求方式 | `POST` |
| 接口描述 | 创建用户自定义事件 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `dto` | UserCustomEventCreateDTO | 是 | 请求体 | 创建DTO |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 9. 获取用户自定义事件列表

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 获取用户自定义事件列表 |
| 请求地址 | `http://localhost:7777/api/v1/festival/custom-events` |
| 请求方式 | `GET` |
| 接口描述 | 获取用户自定义事件列表 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 10. 获取用户即将到来的事件

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 获取用户即将到来的事件 |
| 请求地址 | `http://localhost:7777/api/v1/festival/custom-events/upcoming` |
| 请求方式 | `GET` |
| 接口描述 | 获取用户即将到来的事件 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `defaultValue` | = | 是 | 查询参数 | - |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "defaultValue": {}
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 11. 提交推荐反馈

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 提交推荐反馈 |
| 请求地址 | `http://localhost:7777/api/v1/festival/feedback` |
| 请求方式 | `POST` |
| 接口描述 | 提交推荐反馈 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `dto` | RecommendFeedbackDTO | 是 | 请求体 | 反馈DTO |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```


## FileAccessController

### 1. 文件访问控制器（兼容模式） 处理旧的直接

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 文件访问控制器（兼容模式） 处理旧的直接 |
| 请求地址 | `http://localhost:7777/api/files/{filename:.+}` |
| 请求方式 | `GET` |
| 接口描述 | 文件访问控制器（兼容模式） 处理旧的直接文件名访问方式，自动在多个目录中查找文件 支持的访问路径： - /api/files/abc123.png - 标准访问方式 - /api/abc123.png - 兼容旧方式，自动查找 优先级：设置为最高优先级，确保在静态资源处理之前拦截文件访问请求 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | ResponseEntity<Resource> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```


## FileController

### 1. 文件访问控制器 处理所有上传文件的访问请

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 文件访问控制器 处理所有上传文件的访问请 |
| 请求地址 | `http://localhost:7777/api/files/**` |
| 请求方式 | `GET` |
| 接口描述 | 文件访问控制器 处理所有上传文件的访问请求 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `required` | = | 是 | 路径参数 | - |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | ResponseEntity<Resource> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "required": {}
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 2. 访问上传的图片（兼容旧接口） 路径格式：

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 访问上传的图片（兼容旧接口） 路径格式： |
| 请求地址 | `http://localhost:7777/api/files/image/{filename}` |
| 请求方式 | `GET` |
| 接口描述 | 访问上传的图片（兼容旧接口） 路径格式：/files/image/{filename} |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | ResponseEntity<Resource> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 3. 访问聊天图片 路径格式：/files/c

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 访问聊天图片 路径格式：/files/c |
| 请求地址 | `http://localhost:7777/api/files/chat/{filename}` |
| 请求方式 | `GET` |
| 接口描述 | 访问聊天图片 路径格式：/files/chat/{filename} |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | ResponseEntity<Resource> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```


## HomeController

### 1. 首页控制器

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 首页控制器 |
| 请求地址 | `http://localhost:7777/api/v1/home/hot-topic` |
| 请求方式 | `GET` |
| 接口描述 | 首页控制器 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 2. 记录热点点击

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 记录热点点击 |
| 请求地址 | `http://localhost:7777/api/v1/home/hot-topic/click` |
| 请求方式 | `POST` |
| 接口描述 | 记录热点点击 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `params` | Map<T> | 是 | 请求体 | 请求参数，包含 content 字段 |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 3. 记录热点分享

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 记录热点分享 |
| 请求地址 | `http://localhost:7777/api/v1/home/hot-topic/share` |
| 请求方式 | `POST` |
| 接口描述 | 记录热点分享 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `params` | Map<T> | 是 | 请求体 | 请求参数，包含 content 字段 |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```


## HotTopicAdminController

### 1. 热点管理后台控制器

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 热点管理后台控制器 |
| 请求地址 | `http://localhost:7777/api/v1/admin/hot-topic` |
| 请求方式 | `GET` |
| 接口描述 | 热点管理后台控制器 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `defaultValue` | = | 是 | 查询参数 | - |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `records` | array | 数据列表 |
| `total` | integer | 总记录数 |
| `current` | integer | 当前页码 |
| `size` | integer | 每页大小 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "defaultValue": {}
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 2. 获取热点详情

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 获取热点详情 |
| 请求地址 | `http://localhost:7777/api/v1/admin/hot-topic/detail/{id}` |
| 请求方式 | `GET` |
| 接口描述 | 获取热点详情 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | ResponseEntity<Map<String, Object>> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 3. 创建热点

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 创建热点 |
| 请求地址 | `http://localhost:7777/api/v1/admin/hot-topic/create` |
| 请求方式 | `POST` |
| 接口描述 | 创建热点 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `hotTopic` | HotTopic | 是 | 请求体 | 请求体 |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | ResponseEntity<Map<String, Object>> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 4. 更新热点

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 更新热点 |
| 请求地址 | `http://localhost:7777/api/v1/admin/hot-topic/update/{id}` |
| 请求方式 | `PUT` |
| 接口描述 | 更新热点 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `hotTopic` | HotTopic | 是 | 请求体 | 请求体 |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | ResponseEntity<Map<String, Object>> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 5. 删除热点

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 删除热点 |
| 请求地址 | `http://localhost:7777/api/v1/admin/hot-topic/delete/{id}` |
| 请求方式 | `DELETE` |
| 接口描述 | 删除热点 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | ResponseEntity<Map<String, Object>> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 6. 审核热点

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 审核热点 |
| 请求地址 | `http://localhost:7777/api/v1/admin/hot-topic/review/{id}` |
| 请求方式 | `POST` |
| 接口描述 | 审核热点 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `required` | = | 是 | 查询参数 | - |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | ResponseEntity<Map<String, Object>> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "required": {}
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 7. 批量删除热点

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 批量删除热点 |
| 请求地址 | `http://localhost:7777/api/v1/admin/hot-topic/batch-delete` |
| 请求方式 | `DELETE` |
| 接口描述 | 批量删除热点 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `ids` | List<T> | 是 | 请求体 | 请求体 |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | ResponseEntity<Map<String, Object>> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 8. 获取统计数据

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 获取统计数据 |
| 请求地址 | `http://localhost:7777/api/v1/admin/hot-topic/statistics` |
| 请求方式 | `GET` |
| 接口描述 | 获取统计数据 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | ResponseEntity<Map<String, Object>> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```


## MockOAuthController

### 1. 模拟 OAuth 授权服务器（毕设演示用

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 模拟 OAuth 授权服务器（毕设演示用 |
| 请求地址 | `http://localhost:7777/api/mock/oauth/{provider}/authorize` |
| 请求方式 | `GET` |
| 接口描述 | 模拟 OAuth 授权服务器（毕设演示用） 模拟微信/QQ的OAuth2.0授权流程： 1. 显示模拟授权页面（GET /mock/oauth/{provider}/authorize） 2. 用户输入模拟账号密码确认授权（POST /mock/oauth/{provider}/authorize） 3. 用 code 换 token（GET /mock/oauth/{provider}/token） 4. 用 token 获取用户信息（GET /mock/oauth/{provider}/userinfo） |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | String | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 2. 处理模拟授权表单提交

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 处理模拟授权表单提交 |
| 请求地址 | `http://localhost:7777/api/mock/oauth/{provider}/authorize` |
| 请求方式 | `POST` |
| 接口描述 | 处理模拟授权表单提交，生成 code 并重定向 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `required` | = | 是 | 查询参数 | - |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | String | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "required": {}
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 3. 模拟用 code 换 access_to

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 模拟用 code 换 access_to |
| 请求地址 | `http://localhost:7777/api/mock/oauth/{provider}/token` |
| 请求方式 | `GET` |
| 接口描述 | 模拟用 code 换 access_token 返回格式与真实平台一致 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | Map<String, Object> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 4. 模拟用 access_token 获取用

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 模拟用 access_token 获取用 |
| 请求地址 | `http://localhost:7777/api/mock/oauth/{provider}/userinfo` |
| 请求方式 | `GET` |
| 接口描述 | 模拟用 access_token 获取用户信息 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | Map<String, Object> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```


## PaymentController

### 1. 支付控制器

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 支付控制器 |
| 请求地址 | `http://localhost:7777/api/v1/payment/order/{orderId}` |
| 请求方式 | `GET` |
| 接口描述 | 支付控制器 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 2. 获取可用优惠券 PAY-002: 调用A

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 获取可用优惠券 PAY-002: 调用A |
| 请求地址 | `http://localhost:7777/api/v1/payment/coupons` |
| 请求方式 | `GET` |
| 接口描述 | 获取可用优惠券 PAY-002: 调用API获取可用优惠券 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 3. 创建支付订单 PAY-003: 调用统一

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 创建支付订单 PAY-003: 调用统一 |
| 请求地址 | `http://localhost:7777/api/v1/payment/create` |
| 请求方式 | `POST` |
| 接口描述 | 创建支付订单 PAY-003: 调用统一支付API |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `request` | Map<T> | 是 | 请求体 | 请求体 |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 4. 微信支付 PAY-004: 调用微信支付

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 微信支付 PAY-004: 调用微信支付 |
| 请求地址 | `http://localhost:7777/api/v1/payment/wechat` |
| 请求方式 | `POST` |
| 接口描述 | 微信支付 PAY-004: 调用微信支付 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `request` | Map<T> | 是 | 请求体 | 请求体 |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 5. 支付宝支付 PAY-005: 调用支付宝

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 支付宝支付 PAY-005: 调用支付宝 |
| 请求地址 | `http://localhost:7777/api/v1/payment/alipay` |
| 请求方式 | `POST` |
| 接口描述 | 支付宝支付 PAY-005: 调用支付宝支付 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `request` | Map<T> | 是 | 请求体 | 请求体 |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 6. 余额支付 PAY-006: 调用余额支付

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 余额支付 PAY-006: 调用余额支付 |
| 请求地址 | `http://localhost:7777/api/v1/payment/balance` |
| 请求方式 | `POST` |
| 接口描述 | 余额支付 PAY-006: 调用余额支付API |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `request` | Map<T> | 是 | 请求体 | 请求体 |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 7. 查询支付状态 PAY-007: 轮询查询

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 查询支付状态 PAY-007: 轮询查询 |
| 请求地址 | `http://localhost:7777/api/v1/payment/status/{paymentNo}` |
| 请求方式 | `GET` |
| 接口描述 | 查询支付状态 PAY-007: 轮询查询支付状态 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 8. 查询订单支付状态（通过订单ID）

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 查询订单支付状态（通过订单ID） |
| 请求地址 | `http://localhost:7777/api/v1/payment/order/{orderId}/status` |
| 请求方式 | `GET` |
| 接口描述 | 查询订单支付状态（通过订单ID） |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```


## RemarkConflictController

### 1. 检查conflict

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 检查conflict |
| 请求地址 | `http://localhost:7777/api/v1/remark-conflict/check` |
| 请求方式 | `POST` |
| 接口描述 | 检查conflict |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `dto` | RemarkConflictCheckDTO | 是 | 请求体 | 请求体 |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 2. 解析tastetags

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 解析tastetags |
| 请求地址 | `http://localhost:7777/api/v1/remark-conflict/parse-tags` |
| 请求方式 | `POST` |
| 接口描述 | 解析tastetags |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 3. 获取recommendedtasteta

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 获取recommendedtasteta |
| 请求地址 | `http://localhost:7777/api/v1/remark-conflict/recommended-tags/{dishId}` |
| 请求方式 | `GET` |
| 接口描述 | 获取recommendedtastetags |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 4. 格式化remark

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 格式化remark |
| 请求地址 | `http://localhost:7777/api/v1/remark-conflict/format` |
| 请求方式 | `POST` |
| 接口描述 | 格式化remark |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```


## SupervisorAgentController

### 1. SupervisorAgent 控制器（

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | SupervisorAgent 控制器（ |
| 请求地址 | `http://localhost:7777/api/agent/supervisor/chat` |
| 请求方式 | `POST` |
| 接口描述 | SupervisorAgent 控制器（V2 架构） 提供 Supervisor + StreamingResponse 的同步接口（UniApp 端） 执行流程： 1. 同步阶段：SupervisorAgent 协调 L1 专家 Agent 收集数据 2. 流式阶段：StreamingResponseAgent 格式化输出（同步收集完整结果） |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `request` | ChatRequest | 是 | 请求体 | 请求体 |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 2. 带用户上下文的聊天接口

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 带用户上下文的聊天接口 |
| 请求地址 | `http://localhost:7777/api/agent/supervisor/chatWithContext` |
| 请求方式 | `POST` |
| 接口描述 | 带用户上下文的聊天接口 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `request` | ChatRequest | 是 | 请求体 | 请求体 |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 3. GET方式的快速聊天接口

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | GET方式的快速聊天接口 |
| 请求地址 | `http://localhost:7777/api/agent/supervisor/chat` |
| 请求方式 | `GET` |
| 接口描述 | GET方式的快速聊天接口 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```


## SupervisorSSEController

### 1. SupervisorAgent SSE 

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | SupervisorAgent SSE  |
| 请求地址 | `http://localhost:7777/api/agent/supervisor-sse/chat` |
| 请求方式 | `GET` |
| 接口描述 | SupervisorAgent SSE 流式输出控制器（V2 架构） 架构：同步 Supervisor（数据收集）+ 流式 Response（逐字输出） 执行流程： 1. 同步阶段：SupervisorAgent 协调 L1 专家 Agent 收集数据（用户看到进度事件） 2. 流式阶段：StreamingResponseAgent 将结果逐字输出（用户看到打字机效果） SSE 事件格式（兼容前端 useStreamResponse.js）： - 进度事件: {"message":"正在搜索菜品","progress":true} → 前端过滤跳过 - Token事件: {"content":"推"} → 前端逐字追加 - 完成事件: {"done":true} → 前端触发 onComplete |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | SseEmitter | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 2. 处理客服助手对话（无个性化服务） pri

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 处理客服助手对话（无个性化服务） pri |
| 请求地址 | `http://localhost:7777/api/agent/supervisor-sse/chat` |
| 请求方式 | `POST` |
| 接口描述 | 处理客服助手对话（无个性化服务） private SseEmitter handleCustomerServiceChat(String message) { SseEmitter emitter = new SseEmitter(SSE_TIMEOUT_MS); CompletableFuture.runAsync(() -> { try { String response = customerServiceAgent.chat(message); sendSseEvent(emitter, "message", Map.of("content", response)); sendSseEvent(emitter, "message", Map.of("done", true)); } catch (Exception e) { log.error("客服助手处理失败", e); sendSseEvent(emitter, "message", Map.of("error", e.getMessage())); emitter.completeWithError(e); } finally { try { emitter.complete(); } catch (Exception ignored) {} } }, executorService); emitter.onTimeout(() -> emitter.complete()); emitter.onError(error -> emitter.completeWithError(Objects.requireNonNull(error))); return emitter; } 处理简单对话（快速通道） 使用 SimpleChatAgent 直接调用 LLM，无需走 SupervisorAgent 全流程 private SseEmitter handleSimpleChat(String message) { SseEmitter emitter = new SseEmitter(SSE_TIMEOUT_MS); CompletableFuture.runAsync(() -> { try { long startTime = System.currentTimeMillis(); String response = simpleChatAgent.chat(message); long elapsed = System.currentTimeMillis() - startTime; log.info("⏱️ [快速通道] SimpleChatAgent完成，耗时: {}ms，结果长度: {} 字符", elapsed, response.length()); sendSseEvent(emitter, "message", Map.of("content", response)); sendSseEvent(emitter, "message", Map.of("done", true)); } catch (Exception e) { log.error("简单对话处理失败", e); sendSseEvent(emitter, "message", Map.of("error", "抱歉，回复出了点问题，请稍后重试")); sendSseEvent(emitter, "message", Map.of("done", true)); } finally { try { emitter.complete(); } catch (Exception ignored) {} } }, executorService); emitter.onTimeout(() -> emitter.complete()); emitter.onError(error -> emitter.completeWithError(Objects.requireNonNull(error))); return emitter; } 处理 SupervisorAgent + StreamingResponse 对话（核心方法） 执行流程： 阶段1（同步）：SupervisorAgent 协调 L1 专家 Agent，进度事件实时推送 阶段2（流式）：StreamingResponseAgent 逐字输出，每个 token 作为 SSE 事件发送 private SseEmitter handleSupervisorChat(String message, String userId) { SseEmitter emitter = new SseEmitter(SSE_TIMEOUT_MS); // 创建监听器（进度推送） SSEAgentListener listener = new SSEAgentListener(emitter, userId, message); CompletableFuture.runAsync(() -> { long totalStartTime = System.currentTimeMillis(); try { // ===== 阶段1：同步 Supervisor 执行 ===== log.info("[阶段1] SupervisorAgent开始处理: userId={}, message={}", userId, message); long t1 = System.currentTimeMillis(); SupervisorAgent agent = supervisorAgentFactory.createWithListener(listener, userId); long t1Create = System.currentTimeMillis(); log.info("⏱️ [耗时] SupervisorAgent创建: {}ms", t1Create - t1); String supervisorResult = agent.invoke(message); long t1Invoke = System.currentTimeMillis(); log.info("⏱️ [耗时] SupervisorAgent.invoke(): {}ms", t1Invoke - t1Create); // 清理 LangChain4j 调试信息 String cleanedResult = supervisorAgentFactory.cleanDebugInfo(supervisorResult); long t1Clean = System.currentTimeMillis(); log.info("⏱️ [耗时] cleanDebugInfo: {}ms", t1Clean - t1Invoke); log.info("[阶段1] SupervisorAgent完成，结果长度: {} 字符，总耗时: {}ms", cleanedResult.length(), t1Clean - t1); // ===== 预提取卡片数据 ===== // 从 ToolExecutionContext 中提取工具返回的卡片数据，直接发送给前端 // 不依赖 Supervisor LLM 或 StreamingResponseAgent LLM 透传 java.util.List<String> preExtractedCards = com.xx.jaseatschoicejava.agent.context.ToolExecutionContext.extractCardJsonData(); boolean hasPreExtractedCards = !preExtractedCards.isEmpty(); if (hasPreExtractedCards) { log.info("[预提取] 从工具结果中提取到 {} 张卡片，直接发送给前端", preExtractedCards.size()); for (String cardJson : preExtractedCards) { sendSseEvent(emitter, "message", Map.of("card_data", cardJson, "type", "card")); } } // ===== 阶段2：直接输出阶段1结果 ===== log.info("[阶段2] 直接输出阶段1结果: userId={}", userId); boolean hasResultCards = !preExtractedCards.isEmpty(); emitFinalResponse(emitter, cleanedResult, hasResultCards); long totalEndTime = System.currentTimeMillis(); log.info("⏱️ [耗时] 总耗时: {}ms (阶段1={}ms, 阶段2={}ms)", totalEndTime - totalStartTime, t1Clean - t1, totalEndTime - t1Clean); try { emitter.complete(); } catch (Exception ignored) {} log.info("Supervisor + Direct Response 完成: userId={}", userId); } catch (Exception e) { log.error("SupervisorAgent处理失败: userId={}, 错误类型={}", userId, e.getClass().getSimpleName(), e); handleSupervisorFailure(emitter, message, userId, e); } finally { // 清理工具执行上下文，防止 ThreadLocal 泄漏 com.xx.jaseatschoicejava.agent.context.ToolExecutionContext.clear(); } }, executorService); // 设置超时和错误回调 emitter.onTimeout(() -> { log.warn("SSE连接超时: userId={}", userId); try { emitter.complete(); } catch (Exception ignored) {} }); emitter.onError(error -> { Throwable safeError = Objects.requireNonNull(error); log.error("SSE连接错误: userId={}", userId, safeError); try { emitter.completeWithError(safeError); } catch (Exception ignored) {} }); emitter.onCompletion(() -> log.debug("SSE连接完成: userId={}", userId)); return emitter; } POST方式的SSE流式聊天（支持更复杂的请求体） |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `request` | ChatRequest | 是 | 请求体 | 请求体 |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | SseEmitter | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```


## TutorialController

### 1. 教程控制器 - 用户端接口

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 教程控制器 - 用户端接口 |
| 请求地址 | `http://localhost:7777/api/v1/tutorial/featured` |
| 请求方式 | `GET` |
| 接口描述 | 教程控制器 - 用户端接口 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | ResponseEntity<List<Tutorial>> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 2. 获取所有已发布的教程 GET /api/

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 获取所有已发布的教程 GET /api/ |
| 请求地址 | `http://localhost:7777/api/v1/tutorial/list` |
| 请求方式 | `GET` |
| 接口描述 | 获取所有已发布的教程 GET /api/v1/tutorial/list |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | ResponseEntity<List<Tutorial>> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 3. 获取教程详情 GET /api/v1/t

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 获取教程详情 GET /api/v1/t |
| 请求地址 | `http://localhost:7777/api/v1/tutorial/{id}` |
| 请求方式 | `GET` |
| 接口描述 | 获取教程详情 GET /api/v1/tutorial/{id} |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | ResponseEntity<Tutorial> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 4. 分页查询教程 GET /api/v1/t

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 分页查询教程 GET /api/v1/t |
| 请求地址 | `http://localhost:7777/api/v1/tutorial/page` |
| 请求方式 | `GET` |
| 接口描述 | 分页查询教程 GET /api/v1/tutorial/page?page=1&size=10&sourceType=ADMIN&status=PUBLISHED |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `defaultValue` | = | 是 | 查询参数 | - |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `records` | array | 数据列表 |
| `total` | integer | 总记录数 |
| `current` | integer | 当前页码 |
| `size` | integer | 每页大小 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "defaultValue": {}
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```


## TutorialMerchantController

### 1. 教程商家控制器 - 商家接口

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 教程商家控制器 - 商家接口 |
| 请求地址 | `http://localhost:7777/api/v1/tutorial/merchant/create` |
| 请求方式 | `POST` |
| 接口描述 | 教程商家控制器 - 商家接口 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `tutorial` | Tutorial | 是 | 请求体 | 请求体 |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | ResponseEntity<Tutorial> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 2. 商家更新教程 PUT /api/v1/t

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 商家更新教程 PUT /api/v1/t |
| 请求地址 | `http://localhost:7777/api/v1/tutorial/merchant/{id}` |
| 请求方式 | `PUT` |
| 接口描述 | 商家更新教程 PUT /api/v1/tutorial/merchant/{id} |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `tutorial` | Tutorial | 是 | 请求体 | 请求体 |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | ResponseEntity<Map<String, Object>> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 3. 商家提交审核 POST /api/v1/

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 商家提交审核 POST /api/v1/ |
| 请求地址 | `http://localhost:7777/api/v1/tutorial/merchant/{id}/submit` |
| 请求方式 | `POST` |
| 接口描述 | 商家提交审核 POST /api/v1/tutorial/merchant/{id}/submit |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | ResponseEntity<Map<String, Object>> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 4. 获取商家的教程列表 GET /api/v

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 获取商家的教程列表 GET /api/v |
| 请求地址 | `http://localhost:7777/api/v1/tutorial/merchant/my` |
| 请求方式 | `GET` |
| 接口描述 | 获取商家的教程列表 GET /api/v1/tutorial/merchant/my?page=0&size=10 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `defaultValue` | = | 是 | 查询参数 | - |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `records` | array | 数据列表 |
| `total` | integer | 总记录数 |
| `current` | integer | 当前页码 |
| `size` | integer | 每页大小 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "defaultValue": {}
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 5. 删除自己的教程（仅草稿状态） DELET

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 删除自己的教程（仅草稿状态） DELET |
| 请求地址 | `http://localhost:7777/api/v1/tutorial/merchant/{id}` |
| 请求方式 | `DELETE` |
| 接口描述 | 删除自己的教程（仅草稿状态） DELETE /api/v1/tutorial/merchant/{id} |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | ResponseEntity<Map<String, Object>> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```


## TutorialStatisticsController

### 1. 教程统计控制器

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 教程统计控制器 |
| 请求地址 | `http://localhost:7777/api/v1/tutorial/stats/{id}/view` |
| 请求方式 | `POST` |
| 接口描述 | 教程统计控制器 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | ResponseEntity<Map<String, Object>> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 2. 用户评分 POST /api/v1/tu

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 用户评分 POST /api/v1/tu |
| 请求地址 | `http://localhost:7777/api/v1/tutorial/stats/{id}/rating` |
| 请求方式 | `POST` |
| 接口描述 | 用户评分 POST /api/v1/tutorial/stats/{id}/rating Body: { "rating": 4.5 } |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `request` | Map<T> | 是 | 请求体 | 请求体 |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | ResponseEntity<Map<String, Object>> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 3. 收藏教程 POST /api/v1/tu

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 收藏教程 POST /api/v1/tu |
| 请求地址 | `http://localhost:7777/api/v1/tutorial/stats/{id}/favorite` |
| 请求方式 | `POST` |
| 接口描述 | 收藏教程 POST /api/v1/tutorial/stats/{id}/favorite |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `data` | ResponseEntity<Map<String, Object>> | 返回数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```


## VerificationController

### 1. 发送verificationcode

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 发送verificationcode |
| 请求地址 | `http://localhost:7777/api/v1/verification/send` |
| 请求方式 | `POST` |
| 接口描述 | 发送verificationcode |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 2. 验证验证码

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 验证验证码 |
| 请求地址 | `http://localhost:7777/api/v1/verification/verify` |
| 请求方式 | `POST` |
| 接口描述 | 验证验证码 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```


## WalletSecurityController

### 1. 更新walletlockstatus

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 更新walletlockstatus |
| 请求地址 | `http://localhost:7777/api/v1/wallet/lock-status` |
| 请求方式 | `PUT` |
| 接口描述 | 更新walletlockstatus |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 2. 获取walletsecuritysett

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 获取walletsecuritysett |
| 请求地址 | `http://localhost:7777/api/v1/wallet/security-settings` |
| 请求方式 | `GET` |
| 接口描述 | 获取walletsecuritysettings |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```


## WishListController

### 1. 想吃列表控制器

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 想吃列表控制器 |
| 请求地址 | `http://localhost:7777/api/v1/wish-list/item` |
| 请求方式 | `POST` |
| 接口描述 | 想吃列表控制器 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `dto` | WishListItemCreateDTO | 是 | 请求体 | 创建DTO |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 2. 获取用户的想吃列表

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 获取用户的想吃列表 |
| 请求地址 | `http://localhost:7777/api/v1/wish-list/items` |
| 请求方式 | `GET` |
| 接口描述 | 获取用户的想吃列表 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 3. 获取商家待审核列表

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 获取商家待审核列表 |
| 请求地址 | `http://localhost:7777/api/v1/wish-list/merchant/pending` |
| 请求方式 | `GET` |
| 接口描述 | 获取商家待审核列表 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 4. 审核想吃列表项

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 审核想吃列表项 |
| 请求地址 | `http://localhost:7777/api/v1/wish-list/audit` |
| 请求方式 | `POST` |
| 接口描述 | 审核想吃列表项 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `dto` | WishListAuditDTO | 是 | 请求体 | 审核DTO |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 5. 用户申诉

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 用户申诉 |
| 请求地址 | `http://localhost:7777/api/v1/wish-list/appeal` |
| 请求方式 | `POST` |
| 接口描述 | 用户申诉 |

#### 请求参数

| 参数名 | 类型 | 必填 | 位置 | 说明 |
| --- | --- | --- | --- | --- |
| `dto` | WishListAppealDTO | 是 | 请求体 | 申诉DTO |

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{
  "// 请求体": "根据实际业务填写"
}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 6. 撤回想吃列表项

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 撤回想吃列表项 |
| 请求地址 | `http://localhost:7777/api/v1/wish-list/item/{itemId}` |
| 请求方式 | `DELETE` |
| 接口描述 | 撤回想吃列表项 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 7. 获取列表项详情

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 获取列表项详情 |
| 请求地址 | `http://localhost:7777/api/v1/wish-list/item/{itemId}` |
| 请求方式 | `GET` |
| 接口描述 | 获取列表项详情 |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

### 8. 自动审核超时项（定时任务调用）

#### 接口基本信息

| 项目 | 内容 |
| --- | --- |
| 接口名称 | 自动审核超时项（定时任务调用） |
| 请求地址 | `http://localhost:7777/api/v1/wish-list/auto-audit-timeout` |
| 请求方式 | `POST` |
| 接口描述 | 自动审核超时项（定时任务调用） |

#### 请求参数

无请求参数。

#### 响应参数

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `success` | boolean | 是否成功 |
| `code` | string | 业务状态码 |
| `message` | string | 提示信息 |
| `data` | object | 业务数据 |

#### 异常响应

参见[通用异常响应](#通用异常响应)。

#### 请求示例

```json
{}
```

#### 响应示例

```json
{
  "success": true,
  "code": "200",
  "message": "操作成功",
  "data": {}
}
```

