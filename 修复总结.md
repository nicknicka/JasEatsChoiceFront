已修复lambda表达式引用非最终变量的错误。由于系统中没有安装Gradle或Maven，无法直接启动后端服务。错误修复后，您可以使用自己的构建工具重新构建并启动后端服务。

修复内容：
1. 将原来的hourStart和hourEnd变量替换为finalStart和finalEnd
2. 确保lambda表达式中引用的变量是最终变量或实际上的最终变量
3. 这样就解决了'从lambda 表达式引用的本地变量必须是最终变量或实际上的最终变量'的错误

建议：
1. 安装Gradle或Maven
2. 使用构建工具构建并启动后端服务
3. 确保前端API配置与后端一致
