# 小程序端首轮主流程问题清单

## 记录时间

- 2026-05-06

## 本轮范围

- 仅覆盖用户端主流程
- 主验证目标仍然是微信小程序构建产物
- `H5` 仅作为辅助截图和宿主排障面，不作为本轮通过标准

## 已补充的关键页面日志

- 首页：`src/pages/home/index/index.vue`
- 食谱：`src/pages/recipe/index.vue`
- AI 助手：`src/pages/ai/index.vue`
- 消息中心：`src/pages/message/index.vue`
- 我的：`src/pages/profile/user-center/index.vue`
- 商家详情：`src/pages-user/merchant/detail/index.vue`
- 菜品详情：`src/pages-user/dish/detail/index.vue`
- 购物车：`src/pages-user/cart/index.vue`
- 确认订单：`src/pages-user/order/confirm/index.vue`
- 订单列表：`src/pages-user/orders/index.vue`
- 订单详情：`src/pages-user/order/detail/index.vue`
- 通用日志工具：`src/utils/page-debug.js`

## 截图与验证材料

- `H5` 阻断截图目录：`JasEatsChoiceUniApp/.artifacts/h5-check/`
- 小程序联调截图目录：`JasEatsChoiceUniApp/.artifacts/mp-weixin-check/`
- 当前已有截图：
  - `.artifacts/h5-check/home-probe.png`
  - `.artifacts/h5-check/home-built-probe2.png`
  - `.artifacts/mp-weixin-check/splash-stuck.png`
  - `.artifacts/mp-weixin-check/login-loading-after-wechat-fix.png`
  - `.artifacts/mp-weixin-check/home-after-login.png`
- `.artifacts/mp-weixin-check/reload-splash-clean-console.png`
- `.artifacts/mp-weixin-check/home-compile-mode-empty.png`
- 说明：
  - 本轮已通过微信开发者工具实际完成登录、首页、菜品详情节点联调
  - 仍有部分页面需要继续点透和补拍回归截图

## 本轮新增实测结论

- 微信小程序开发版已完成实际启动，不再只停留在构建通过
- 微信登录链路已能从登录页进入首页
- 首页推荐菜品入口已能进入菜品详情页
- 菜品详情 -> 购物车 -> 确认订单链路已在微信开发者工具中实际点通
- 购物车 -> 确认订单 -> 创建订单链路已在微信开发者工具中实际触发
- 当前仍存在首屏图片、菜品详情数据映射、购物车加购、启动页稳定性等问题
- 当前已补充“首页调试”编译模式用于绕开启动页阻断，但开发者工具仍存在页面主体偶发空白
- 首页外网占位图报错已消失，说明主包首页的图片兜底已开始生效
- 主包首页在直达编译模式下仍会把推荐接口打成 `userId=undefined`
- 后端上传图片资源仍存在缺失，当前会回退到本地占位图或显示默认块

## 本轮已修复

### 1. 首页入口链路

- 页面/入口：首页 Banner、分类入口、更多商家
- 复现步骤：进入首页后点击 Banner、分类卡片、更多商家
- 原实际结果：部分跳转写成 `/src/pages-user/...`，属于源码路径
- 预期结果：跳转到 `pages.json` 中真实注册的用户分包页面
- 分类：路径
- 是否已修复：已修复
- 修复后回归结果：首页相关入口统一改为真实注册路径或路由工具
- 剩余风险：仍需在微信开发者工具里逐个点按确认真实数据下的跳转参数

### 2. 商家详情主流程

- 页面/入口：商家详情页分享、菜品详情跳转、购物车、立即下单
- 复现步骤：从首页进入商家详情，执行分享、点菜、加购、下单
- 原实际结果：
  - 分享逻辑引用未定义的 `merchantInfo`
  - 去菜品详情、购物车、确认订单的链路不稳定
- 预期结果：商家详情页可继续进入后续下单链路
- 分类：逻辑 + 路径
- 是否已修复：已修复
- 修复后回归结果：
  - 分享数据改为使用当前页面真实数据对象
  - 跳转改为路由工具或真实分包路径
  - 下单临时数据构造补齐商家信息
- 剩余风险：依赖后端返回的商家/菜品数据完整性

### 3. 菜品详情主流程

- 页面/入口：菜品详情页去商家、去购物车、加入购物车
- 复现步骤：进入菜品详情后点击商家入口、购物车入口、加购
- 原实际结果：仍混用旧路径，后续链路容易断
- 预期结果：菜品详情页能够继续进入商家详情和购物车
- 分类：路径 + 逻辑
- 是否已修复：已修复
- 修复后回归结果：
  - 跳转统一接到路由工具
  - 加购时补齐商家名，避免购物车数据结构残缺
- 剩余风险：规格字段为空时，仍需在真机或开发者工具里验证边界行为

### 4. 购物车与订单链路

- 页面/入口：购物车、确认订单、订单列表、订单详情、支付回跳
- 复现步骤：加购后进入购物车，提交订单，再查看订单列表和详情
- 原实际结果：
  - 购物车页面按错误的数据结构读取仓库
  - 去首页、确认订单、订单详情、评价、支付回跳存在旧路径
  - 订单详情金额字段映射和模板字段不一致
- 预期结果：用户主下单链路可连续推进
- 分类：逻辑 + 路径
- 是否已修复：已修复
- 修复后回归结果：
  - 购物车页按真实仓库结构取数
  - 仓库补了兼容方法，支持旧页面调用方式
  - 订单链路跳转统一切到真实用户分包路径
  - 订单详情金额字段映射恢复一致
  - 支付成功后回跳首页/订单详情的路径已修正
- 剩余风险：支付接口真实返回结构仍需在开发者工具联调验证

### 5. 搜索、收藏、历史、食谱、个人中心入口

- 页面/入口：搜索结果、收藏、历史、食谱详情、我的页菜单
- 复现步骤：从首页/我的进入这些页面并继续点详情或返回首页
- 原实际结果：多处仍使用旧的 `/pages/...` 或不一致路径
- 预期结果：这些入口至少能进入真实注册页面
- 分类：路径
- 是否已修复：已修复
- 修复后回归结果：
  - 搜索结果改为走路由工具
  - 收藏、历史的菜品/商家详情与回首页路径已修正
  - 我的页主要入口映射已对齐 `pages.json`
- 剩余风险：
  - `health-report`
  - `customer-service`
  这两个入口当前仍无真实页面注册，只能保留为“页面开发中”

### 6. AI 助手首屏布局

- 页面/入口：AI 助手
- 复现步骤：切到 AI 页观察欢迎区、消息区、输入区
- 原实际结果：欢迎态失效，聊天区高度塌陷，输入区停在上半屏
- 预期结果：欢迎态、消息区、输入区恢复正常布局
- 分类：逻辑 + 体验
- 是否已修复：已修复
- 修复后回归结果：聊天区改回可滚动区域，输入区贴底，欢迎页逻辑恢复
- 剩余风险：还需要开发者工具里做视觉复验

### 7. 微信登录链路阻断

- 页面/入口：登录页微信一键登录
- 复现步骤：勾选协议后点击微信一键登录
- 原实际结果：
  - 前端请求 `POST /v1/users/wechat-login`
  - 后端该地址未提供可用的 `POST` 处理，返回 `405`
- 预期结果：登录成功并进入首页
- 分类：逻辑
- 是否已修复：已修复
- 修复后回归结果：
  - 后端补了开发期可用的微信登录入口
  - 已在微信开发者工具中实际验证可从登录页进入首页
- 剩余风险：
  - 当前实现是为联调兜底，不是真实微信授权方案
  - 后续若切正式授权，还需替换为真实 `code/openid` 流程

### 8. 菜品详情页加购方法失效

- 页面/入口：首页推荐菜品 -> 菜品详情 -> 加入购物车
- 复现步骤：从首页点击推荐菜品，进入详情页后点击“加入购物车”
- 原实际结果：前端报错 `Cannot read property 'add' of undefined`
- 预期结果：调用购物车接口并更新本地购物车状态
- 分类：逻辑
- 是否已修复：已修复，待回归
- 修复后回归结果：
  - 菜品详情页已改为稳定的购物车接口引用方式
  - 不再使用该处失效的动态解构
- 剩余风险：
  - 刷新后开发者工具再次回到启动页，当前还缺一次干净的页面级回归确认

### 9. 购物车接口缺失导致加购 500

- 页面/入口：菜品详情页 -> 加入购物车
- 复现步骤：进入菜品详情后点击“加入购物车”
- 原实际结果：
  - 前端点击已触发
  - 后端返回 `500`
  - 服务端日志显示 `/v1/cart/items` 没有实际控制器，落到了静态资源处理
- 预期结果：加购成功并继续进入购物车页
- 分类：逻辑
- 是否已修复：已修复，待页面级回归
- 修复后回归结果：
  - 前端购物车接口已增加“后端接口缺失时回落到本地购物车”的降级逻辑
  - 这条链路不再依赖当前不存在的后端购物车实现
- 剩余风险：
  - 仍需在微信开发者工具里完成一次“加购成功 -> 进入购物车”的完整点击回归
  - 后续如果要接真实后端购物车，还需要补服务端接口而不是长期依赖前端降级

### 9. 菜品详情页数据映射错误

- 页面/入口：菜品详情页
- 复现步骤：从首页进入推荐菜品详情
- 原实际结果：
  - 价格显示为 `0`
  - 销量、营养、图片等大面积为空
  - 实际后端已有 `data.price`、`data.image`、`data.name`
- 预期结果：详情页按后端真实返回结构展示基础信息
- 分类：逻辑
- 是否已修复：已修复，待回归
- 修复后回归结果：
  - 前端已按 `res.data || res` 取详情数据
  - 基础图片数组回退逻辑已补上
- 剩余风险：
  - 后端当前仍只返回较少字段，商家名称/评分等展示仍可能为空

### 10. 确认订单页创建订单入参结构不匹配

- 页面/入口：购物车 -> 确认订单 -> 提交订单
- 复现步骤：
  - 在菜品详情页加购
  - 进入购物车并勾选商品
  - 进入确认订单页后点击“提交订单”
- 原实际结果：
  - 前端将平铺结构直接提交到 `/v1/orders`
  - 后端 `OrderController` 需要的是 `OrderCreateDTO { order, dishes }`
  - 服务端返回 `创建订单失败：Cannot invoke "com.xx.jaseatschoicejava.entity.Order.getId()" because "order" is null`
- 预期结果：前端按后端 DTO 结构提交订单，后端能正常创建订单
- 分类：逻辑
- 是否已修复：已修复，待回归
- 修复后回归结果：
  - 确认订单页已改为按 `{ order, dishes }` 结构提交
  - 同时补了地址有效性校验，不再把缺少 `id/name/phone/address` 的空壳地址误判为可下单
- 剩余风险：
  - 热更新后开发者工具编译模式仍不稳定，当前还缺一次“修复后重新提单”的干净回归
  - 后端若还有订单表字段约束问题，需要在下一轮联调继续暴露

### 11. 确认订单页下单后清购物车逻辑报错

- 页面/入口：购物车 -> 确认订单 -> 提交订单
- 复现步骤：
  - 在微信开发者工具里从购物车进入确认订单页
  - 点击“提交订单”并确认支付弹窗
- 原实际结果：
  - 后端 `/v1/orders` 已成功创建订单
  - 前端在创建成功后的清购物车阶段抛出 `_context.sent.useCartStore is not a function`
  - 导致“订单创建成功”后的收尾逻辑被打断
- 预期结果：创建订单成功后，前端正常清理已下单商品并继续后续跳转
- 分类：逻辑
- 是否已修复：已修复
- 修复后回归结果：
  - 确认订单页已改为直接使用静态引入的 `useCartStore`
  - 不再在异步流程里用错误的动态导入方式获取购物车仓库
- 剩余风险：
  - 还需要在更稳定的一次运行态里重新完整点通“提交订单 -> 后续跳转”
  - 当前开发者工具的页面热更新和刷新仍会干扰这条链路的连续验证

## 当前未修复

### 1. 首页轮播图和菜品图片仍大面积加载失败

- 页面/入口：首页
- 复现步骤：登录进入首页，观察轮播区和推荐菜品图
- 实际结果：
  - 轮播图接口返回的图片地址为 `http://localhost:7777/api/uploads/...`
  - 微信开发者工具渲染层提示不再支持 HTTP 图片
  - 后端同时记录部分上传图片资源不存在
  - 推荐菜品默认图走 `via.placeholder.com`，当前也加载失败
- 预期结果：首页图片可稳定加载，至少有本地兜底图
- 分类：逻辑 + 体验
- 是否已修复：未修复
- 修复后回归结果：无
- 剩余风险：首页视觉可用性仍受明显影响

### 2. 购物车和确认订单页的菜品图、商家图仍会加载失败

- 页面/入口：购物车、确认订单
- 复现步骤：
  - 在微信开发者工具里进入购物车页或确认订单页
  - 观察商家图和菜品图
- 实际结果：
  - 本地购物车里残留的是 `via.placeholder.com` 测试图
  - 微信开发者工具渲染层报 `Failed to load image ... net::ERR_CONNECTION_CLOSED`
  - 页面虽然可继续操作，但关键卡片区域会显得明显残缺
- 预期结果：即使远端图片失效，也应显示本地兜底图，不影响页面识别和后续点击
- 分类：逻辑 + 体验
- 是否已修复：未修复
- 修复后回归结果：无
- 剩余风险：
  - 会干扰“页面是否真正渲染成功”的视觉判断
  - 也会持续污染调试日志，降低后续问题定位效率

### 1. 消息通知的活动详情页和通知详情页未落地

- 页面/入口：消息中心通知列表
- 复现步骤：点击活动类通知或普通通知
- 实际结果：当前改为提示“待完善”，不再跳到错误的源码路径
- 预期结果：进入真实的活动详情页或通知详情页
- 分类：功能缺失
- 是否已修复：部分兜底已修，真实页面未补齐
- 修复后回归结果：不会再因为错误路径直接报错
- 剩余风险：功能仍不闭环

### 2. 消息中心仍是静态演示数据

- 页面/入口：消息中心首页
- 复现步骤：进入消息中心查看会话和通知
- 实际结果：当前仍主要依赖本地模拟数据
- 预期结果：接入真实消息与通知数据
- 分类：逻辑
- 是否已修复：未修复
- 修复后回归结果：无
- 剩余风险：只能验证页面样式和静态交互，无法验证真实消息流程

### 3. 启动页稳定性不足

- 页面/入口：启动页
- 复现步骤：开发者工具刷新当前页面或重新走启动链路
- 实际结果：
  - 启动页有时能正常进入登录页/首页
  - 有时会长时间停留在 `pages/splash/index`
  - 当前控制台只剩 `getSystemInfo API 提示`，不容易直接看出阻断点
- 预期结果：启动页应稳定在 2-3 秒内完成登录态判断并跳转
- 分类：逻辑
- 是否已修复：未修复
- 修复后回归结果：无
- 剩余风险：会反复影响后续逐页回归效率

### 4. 首页在“首页调试”编译模式下主体空白

- 页面/入口：微信开发者工具 `首页调试` 编译模式
- 复现步骤：
  - 将编译模式切到“首页调试”
  - 执行编译或刷新首页
- 实际结果：
  - 页面路径已进入 `pages/home/index/index`
  - 顶部导航和底部 tab 存在
  - 页面主体区域偶发整块空白，无法继续通过首页点击进入详情链路
- 预期结果：首页主体正常展示搜索、分类、推荐内容
- 分类：逻辑 + 环境
- 是否已修复：未修复
- 修复后回归结果：无
- 剩余风险：
  - 会阻断“严格按首页真实点击进入菜品详情”的回归路径
  - 需要继续结合控制台日志和编译模式做定向页验证

### 5. 定向编译模式切页后容易出现空白页和宿主异常

- 页面/入口：`首页调试`、`菜品详情调试`、`购物车调试`
- 复现步骤：
  - 在微信开发者工具里切换编译模式
  - 执行编译或刷新页面
- 实际结果：
  - 页面路径经常已经切换成功，但渲染层主体为空白
  - 控制台出现 `TypeError: Cannot read property '__subPageFrameEndTime__' of null`
  - 有时会导致当前调试页恢复到首页或仅剩导航壳层
- 预期结果：定向编译模式能够稳定落到对应页面并完成渲染
- 分类：环境 + 逻辑
- 是否已修复：未修复
- 修复后回归结果：无
- 剩余风险：
  - 会显著拖慢“边跑边修”的回归效率
  - 当前很多页面只能通过真实入口点击进入，不能完全依赖编译模式直达

### 6. 首页推荐区数据偶发为空，阻断从首页进入详情的真实路径

- 页面/入口：首页推荐区
- 复现步骤：
  - 进入首页调试模式
  - 观察推荐区内容
- 实际结果：
  - 推荐区有时能显示 `宫保鸡丁`
  - 有时只有“暂无推荐菜品”和“重新加载”按钮
  - 导致无法稳定从首页真实入口继续点击到菜品详情
- 预期结果：首页推荐区在联调环境下稳定返回至少一条可点击菜品
- 分类：逻辑 + 数据
- 是否已修复：未修复
- 修复后回归结果：无
- 剩余风险：
  - 会影响后续“严格按首页主路径”的自动回归
  - 需要继续确认是推荐接口偶发空数据，还是首页本地筛选/刷新逻辑导致

### 7. 订单列表页原始代码无法消费真实后端返回，已修复

- 页面/入口：`/pages-user/orders/index`
- 复现步骤：
  - 进入订单列表页
  - 触发订单列表加载
- 实际结果：
  - 页面原本调用了不存在的 `orderApi.getList`
  - 即使后端成功返回 `ResponseResult.success(orders)`，前端也只接受数组根对象，导致直接判空
  - 后端用户订单列表接口不带菜品明细，原页面仍按 `order.items` 渲染，商品卡片无法显示
- 预期结果：订单列表页能正确读取用户订单、兼容数字状态、按需补拉订单菜品，并展示订单卡片
- 分类：逻辑
- 是否已修复：已修复
- 修复后回归结果：
  - 源码已改为调用 `orderApi.getByUser`
  - 已兼容 `res.data`
  - 已兼容数字状态映射
  - 已增加订单菜品补拉和本地默认图兜底
- 剩余风险：
  - 受微信开发者工具宿主不稳定影响，当前还没完成最终页面肉眼验收

### 8. 小程序开发态增量产物会把订单列表页编译成空文件

- 页面/入口：`dist/dev/mp-weixin/pages-user/orders/index.*`
- 复现步骤：
  - 启动 `dev:mp-weixin`
  - 切到订单列表页并增量编译
- 实际结果：
  - `index.js`、`index.wxml`、`index.wxss` 会被写成 0 字节空文件
  - 微信开发者工具里表现为只有导航栏标题，页面主体整块空白
- 预期结果：开发态增量编译应输出完整页面产物
- 分类：环境 + 构建
- 是否已修复：未修复
- 修复后回归结果：
  - 完整构建 `build:mp-weixin` 可正常产出非空文件
  - 当前通过停掉不稳定的开发编译并同步稳定构建产物，临时绕过继续联调
- 剩余风险：
  - 只要继续依赖当前开发态增量编译，这个问题就会反复污染联调环境

### 9. 完整构建默认落到生产域名，已修复本地联调配置选择

- 页面/入口：登录页、用户信息拉取、后续所有接口
- 复现步骤：
  - 使用完整构建产物导入微信开发者工具
  - 进入登录页或任意需要接口的页面
- 实际结果：
  - 请求会打到 `https://api.yourdomain.com`
  - 本地联调时出现 `ERR_CONNECTION_CLOSED`
  - 登录态刷新、用户信息读取都会失败
- 预期结果：在微信开发者工具的开发环境里，即使使用完整构建，也应走本地 `http://localhost:7777/api`
- 分类：逻辑 + 环境配置
- 是否已修复：已修复
- 修复后回归结果：
  - 已改为优先识别小程序运行时 `envVersion`
  - 开发者工具里的开发环境会使用本地接口地址
- 剩余风险：
  - 仍需在真机或正式发布环境再核一次生产配置分支

### 10. 真实联调期间，订单会被超时任务自动取消

- 页面/入口：确认订单 -> 已创建待支付订单
- 复现步骤：
  - 创建订单后不继续支付
  - 等待定时任务扫描
- 实际结果：
  - 后端定时任务自动取消了订单 `O2259385358284649`
  - 日志显示“发现1笔超时订单（无支付记录），开始处理”
- 预期结果：联调期应明确知道待支付订单的有效窗口，避免把“订单消失”误判成前端列表故障
- 分类：数据 + 环境
- 是否已修复：未修复
- 修复后回归结果：无
- 剩余风险：
  - 后续验证订单列表时，如果依赖旧订单号，可能会因为订单已取消而误判页面逻辑

### 11. 主包首页推荐接口在直达编译模式下会带 `undefined` 用户 ID，已修复

- 页面/入口：首页调试编译模式
- 复现步骤：
  - 在微信开发者工具里直接打开 `pages/home/index/index`
  - 观察后端推荐接口请求日志
- 实际结果：
  - 后端收到 `GET /api/v1/recommendations/undefined`
  - 说明首页只判断了“已登录”，但没有在用户信息未恢复完成时继续兜底用户 ID
- 预期结果：即使直达首页编译模式下用户状态恢复较慢，也不应把推荐接口打成 `undefined`
- 分类：逻辑
- 是否已修复：已修复
- 修复后回归结果：
  - 首页推荐接口的用户 ID 读取已补上 `userStore.userId` 和默认值兜底
  - 后续需要在开发者工具里再做一次同路径回归确认
- 剩余风险：
  - 如果登录态本身异常，仍可能触发游客兜底推荐而不是真实个性化推荐

### 12. 主包“我的”页仍在使用旧钱包接口路径的代码已修复，待页面级回归

- 页面/入口：`pages/profile/user-center/index`
- 复现步骤：
  - 进入“我的”页
  - 观察钱包信息加载请求
- 原实际结果：
  - 页面代码调用 `userApi.getWalletInfo(userId)`
  - 旧接口路径是 `/v1/users/{userId}/wallet`
  - 这条路径与当前小程序钱包模块使用的 `/v1/wallet/info/{userId}` 不一致
- 预期结果：主包“我的”页与钱包页共用同一套钱包接口
- 分类：逻辑
- 是否已修复：已修复，待页面级回归
- 修复后回归结果：
  - 主包“我的”页已改为调用 `walletApi.getWallet(userId)`
  - 构建产物已确认编译为新接口调用
- 剩余风险：
  - 受微信开发者工具当前串页和超时问题影响，这一页还缺一次干净的页面级请求回归

### 13. 首页轮播图后端上传资源缺失，仍未修复

- 页面/入口：首页轮播图
- 复现步骤：
  - 进入首页
  - 观察轮播图区域和后端请求日志
- 实际结果：
  - 后端持续报 `No static resource 1768052638512-...png`、`1768052638513-...png`
  - 说明当前推荐或轮播依赖的上传图片文件在服务端已缺失
- 预期结果：后端返回的图片资源地址应真实存在，前端能正常加载
- 分类：数据 + 资源
- 是否已修复：未修复
- 修复后回归结果：无
- 剩余风险：
  - 即使前端已补本地默认图，也只能掩盖显示问题，不能恢复真实内容图

## 环境阻断

### 1. `H5` 宿主仍然白屏

- 页面/入口：`H5` 开发态和构建态首页
- 复现步骤：
  - 打开 `http://localhost:5173/#/pages/home/index/index`
  - 或打开静态服务中的构建态首页
- 实际结果：
  - 页面白屏
  - `body` 下只有空的 `#app`
  - 页面级调试日志没有真正进入生命周期
- 预期结果：`H5` 能挂载并进入页面生命周期
- 分类：宿主
- 是否已修复：未修复
- 修复后回归结果：无
- 剩余风险：
  - 浏览器截图核验只能停留在“宿主阻断截图”
  - 不能用浏览器代替微信开发者工具完成本轮主流程复测

### 2. 微信开发者工具全量核验尚未完成

- 页面/入口：本轮全部主流程页面
- 复现步骤：在微信开发者工具导入 `dist/dev/mp-weixin` 或 `dist/build/mp-weixin`
- 实际结果：当前工作流里已完成登录、首页、菜品详情等节点的实际联调，但尚未完成全链路逐页走查
- 预期结果：按首页 -> 商家/菜品 -> 购物车 -> 确认订单 -> 订单列表/详情 -> 食谱 -> AI -> 消息 -> 我的 的顺序人工走查
- 分类：验证缺口
- 是否已修复：未完成
- 修复后回归结果：待人工核验补齐
- 剩余风险：仍可能存在依赖真实接口数据才会暴露的问题

### 3. 微信开发者工具宿主会出现超时和直达页空壳，仍持续干扰真实回归

- 页面/入口：登录页、订单列表页、启动页、定向编译直达页
- 复现步骤：
  - 在开发者工具里切换编译条件或刷新页面
  - 观察控制台和页面主体
- 实际结果：
  - 控制台会出现 `Error: timeout`
  - 有时页面路径正确，但渲染层只剩外壳或白页
  - 有时同一轮操作里登录页能看到，分包页却不进入业务生命周期
- 预期结果：开发者工具应稳定完成页面初始化和路由切换
- 分类：宿主
- 是否已修复：未修复
- 修复后回归结果：无
- 剩余风险：
  - 这是当前阻碍“完整自动点透订单链路”的主要非代码因素

## 编译结果

- 已执行：`npm run build:mp-weixin`
- 结果：构建通过
- 当前输出：
  - `DONE  Build complete.`
- 仍存在的问题：
  - Sass `@import` 和旧版接口弃用警告很多
  - 当前不阻断小程序构建
  - `dev:mp-weixin` 的增量产物稳定性明显差于完整构建

## 已修复 / 未修复 / 被阻断 汇总

### 已修复

- 首页主要入口跳转
- 商家详情主流程跳转与分享变量错误
- 菜品详情去商家/购物车链路
- 微信登录接口缺失导致的首页前置阻断
- 购物车与订单链路的主要路径错误
- 购物车仓库兼容层
- 确认订单页的订单创建入参结构
- 支付成功后的回跳路径
- 搜索、收藏、历史、食谱、我的页主要入口
- AI 助手首屏布局问题
- 完整构建下本地联调环境识别错误，导致请求打到占位生产域名
- 主包首页外网占位图错误加载
- 主包“我的”页旧钱包接口路径
- 主包首页推荐接口 `userId=undefined` 兜底缺失

### 未修复

- 首页轮播图与菜品图片加载失败
- 首页轮播图后端上传资源缺失
- 启动页偶发卡死
- 定向编译模式下的空白页与 `__subPageFrameEndTime__` 异常
- 首页推荐区偶发空数据
- 开发态增量编译会把订单列表页写成空文件
- 订单创建后若不及时支付，会被定时任务自动取消，影响回归数据稳定性
- 消息中心真实数据接入
- 活动详情页、通知详情页真实页面
- 我的页中无真实页面注册的少量保留入口

### 被环境阻断

- `H5` 宿主白屏
- 微信开发者工具 `timeout` / 空壳页问题

## 建议的下一步

1. 优先解决微信开发者工具宿主超时和开发态空产物问题，否则后续每一页都会被环境噪音反复打断。
2. 继续用“登录页 -> 首页 -> 我的 -> 订单”的真实路径回归，不再优先依赖分包页定向直达。
3. 重新创建一笔新的待支付订单，再验证订单列表和订单详情，避免继续使用已被自动取消的旧订单。
4. 在主流程稳定后，再做统一分包收口，不要在本轮中途再做包结构重构。
